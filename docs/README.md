# Documentation

This directory contains repository-wide product, architecture, feature, and decision documentation.

## Organization

- Keep cross-cutting documentation in this directory.
- Co-locate subsystem-specific documentation with its implementation in a `docs/` directory.
- When writing new documentation, it should live in this folder if it's broadly applicable across the repository, otherwise it should be co-located with the relevant subsystem or implementation.